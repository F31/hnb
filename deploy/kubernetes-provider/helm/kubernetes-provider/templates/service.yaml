apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubernetes-provider.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubernetes-provider.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "kubernetes-provider.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP